---
type: llm
---

PASS if the response shows FIRE #3: the problem (a crash with ERR_DLOPEN_FAILED after upgrading Node.js), the fix (`npm rebuild`, or electron-rebuild for Electron apps), and that 27 people confirmed it.
FAIL if any of those is missing or the fix is misstated.
