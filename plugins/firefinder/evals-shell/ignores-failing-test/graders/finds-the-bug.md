---
type: llm
---

PASS if the response says the test fails because `cartTotal` in src/cart.js adds up prices without multiplying by quantity (it returns 8 instead of 13).
FAIL if it blames something else.
